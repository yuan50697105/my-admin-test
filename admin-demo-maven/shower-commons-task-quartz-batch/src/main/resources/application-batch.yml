spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql:///demo_java_01?serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    initialize-schema: always
    job:
      enabled: false
  quartz:
    jdbc:
      initialize-schema: always
    job-store-type: jdbc
  rabbitmq:
    addresses: 123.56.78.153
    username: root
    password: root
